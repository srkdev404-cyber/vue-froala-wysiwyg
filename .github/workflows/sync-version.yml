name: Sync Version from Parent

on:
  repository_dispatch:
    types: [sync-version]

permissions:
  contents: write

jobs:
  sync-version:
    runs-on: ubuntu-latest
    environment: PAT   # <-- makes PAT secret available

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}
          fetch-depth: 0

      - name: Read version from payload
        run: |
          echo "VERSION=${{ github.event.client_payload.version }}" >> $GITHUB_ENV

      - name: Update package.json version
        run: |
          npm version $VERSION --no-git-tag-version

      - name: Install dependencies (first time safe)
        run: npm install

      - name: Commit and push changes
        run: |
          git config user.name "srkdev404-cyber"
          git config user.email "srkdev404@gmail.com"
          git add package.json package-lock.json
          git commit -m "chore: bump version to $VERSION"
          git push origin master
